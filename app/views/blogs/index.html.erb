<div class="container">
  <%= link_to "Create New Blog", new_blog_path, class: :button %>

  <% @blogs.each do |blog| %>
    <%= link_to blog_path(blog), class: "blog-card" do %>
      <div class="blog-title"><%= blog.title %></div>
    <% end %>
  <% end %>
</div>
